# Runtime dependencies — what the app needs to SERVE requests. These are the ONLY
# packages installed into the Docker image. Dev/test tooling lives in
# requirements-dev.txt so it never bloats (or widens the attack surface of) the
# production image.
fastapi
uvicorn[standard]
anthropic
python-dotenv
pydantic-settings   # typed configuration layer (config.py) — reads env vars / .env
boto3               # AWS SDK — DynamoDB conversation storage (storage.py)
